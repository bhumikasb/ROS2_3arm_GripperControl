ROS2 WSG-50 Gripper Control Package Documentation
- `gripper.sh`: A helper shell script to automate building and launching the gripper node.
- `test_gripper_node.py`: Comprehensive unit testing using `pytest`.
- `gripper_control.py`: A sample ROS2 script to command the gripper using a ROS2 service 
call.
